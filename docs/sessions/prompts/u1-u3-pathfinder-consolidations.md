/make-plan

Work type: maintenance commit, bundled. Three Pathfinder
2026-05-15 consolidations (U1, U2, U3) landing as one branch, three
commits, one PR, squash-merge to main per ADR 0006.

Branch: maint/u1-u3-pathfinder-consolidations

Order: U1 → U2 → U3 (smallest leaf first), one commit per
consolidation. After each commit, run the verification block below
before moving to the next.

Each consolidation replaces duplicated private helpers with one shared
module. None of them changes pipeline behaviour or external API
surface; each is byte-equivalent to the existing code per the
PATHFINDER-2026-05-15 duplication report.

Prep step (before any of the three): the v3 quality baseline at
docs/baseline-v3.md is frozen and must not change. The live baseline
at docs/baseline-pipeline.md must stay byte-identical modulo its date
line (the CI guard's `-I '^Generated by'` ignore is what permits the
date drift). If a consolidation changes either file's substantive
content, that is a bug — do not regenerate either file to make the
diff go away; investigate.

==============================================================
U1 — Canonical pair-key helper
==============================================================

Add src/core/pair-key.ts exporting exactly:

  export const canonicalPairKey = (a: number, b: number): string =>
    a < b ? `${a},${b}` : `${b},${a}`;

Rewrites (call sites from PATHFINDER-2026-05-15/02-duplication-report.md, D1):
- src/core/adjacency.ts:39-40 — delete local `canonicalEdgeKey`,
  import `canonicalPairKey` from `./pair-key.js`, update one call
  site at adjacency.ts:53.
- src/core/flatten.ts:37-38 — delete local `canonicalPairKey`,
  import, update call sites at flatten.ts:93 and flatten.ts:123.
- src/core/tabs.ts:30-31 — delete local `canonicalEdgeKey`, import,
  update call sites at tabs.ts:67, tabs.ts:76, tabs.ts:90.

Reject:
- A generic "key utilities" module with a registry of strategies.
- A deprecated alias in any old location. Delete inline helpers cleanly.
- Any change to the key format (tuple, hash, BigInt). It cascades through
  every Map<string, …> in the pipeline.

Commit message: `refactor(core): extract canonicalPairKey to shared module (U1)`

==============================================================
U2 — Union-find helper
==============================================================

Add src/core/union-find.ts exporting:

  export interface UnionFind {
    find: (x: number) => number;
    /** Returns true if a and b were in different sets (the union happened). */
    union: (a: number, b: number) => boolean;
  }
  export function makeUnionFind(n: number): UnionFind { … }

Implementation: lift the existing factory at src/core/spanning-tree.ts:38-77
verbatim. Do not change union-by-rank to union-by-size and do not
touch the path-compression strategy — deterministic output of
buildSpanningTree must be preserved.

Rewrites (call sites from D2):
- src/core/spanning-tree.ts:38-77 — delete local UnionFind interface
  and makeUnionFind factory; import from `./union-find.js`. Usage at
  spanning-tree.ts:101-107 is unchanged.
- src/core/recut.ts:183-208 — inside `connectedComponents`, delete the
  inline ufParent[]/rank[]/find/union block. Replace with:
    const uf = makeUnionFind(faceCount);
    for (const [childFace, fold] of parentFold) {
      if (cuts.has(childFace)) continue;
      uf.union(fold.faceA, fold.faceB);
    }
    // then use uf.find(i) at recut.ts:217
  Discard the boolean return of uf.union; recut doesn't need it.

Reject:
- Generic-typed elements (UnionFind<T>); face indices are number.
- Speculative size()/groups() methods. recut walks find(i) over every i,
  which is fine.
- Algorithm changes (union-by-size, path-halving). Behaviour must be
  byte-identical.

Commit message: `refactor(core): extract makeUnionFind to shared module (U2)`

==============================================================
U3 — Shared vertex interner
==============================================================

Add src/core/intern-vertex.ts exporting:

  export interface VertexInterner {
    intern: (x: number, y: number, z: number) => number;
    readonly vertices: Vec3[];
  }
  export function makeVertexInterner(): VertexInterner { … }

intern MUST validate Number.isFinite on every coordinate and throw a
clear error containing the failed (x, y, z) triple. Key construction
uses toFixed(6) exactly — the 6-decimal convention is the established
cross-parser contract.

Rewrites (call sites from D4):
- src/core/parse-stl.ts:20-32 — delete vertices[], vertexIndex Map,
  internVertex closure; `const v = makeVertexInterner()`. Replace the
  call at parse-stl.ts:48 with `v.intern(x, y, z)`. Drop the local
  finiteness check at parse-stl.ts:44-46 (now inside intern). Return
  `{ vertices: v.vertices, faces }`.
- src/core/parse-obj.ts:18-31 — same pattern. Replace the call at
  parse-obj.ts:69. Drop the local finiteness check at
  parse-obj.ts:64-68. Return `{ vertices: v.vertices, faces }`.

Reject:
- Configurable dedup tolerance ({ decimals: 6 } option). Single global constant.
- A "parse a finite number" helper; Number.isFinite is plenty.
- Error-message rewordings that break unit-test substring assertions.
  Read test/unit/parse-stl.test.ts and test/unit/parse-obj.test.ts BEFORE
  changing the error string; update assertions in the same commit
  if needed.
- Merging the two outer parsers. Format-specific outer loops are
  legitimate specialization per N1.

Commit message: `refactor(core): extract makeVertexInterner to shared module (U3)`

==============================================================
Verification (run after each of the three commits)
==============================================================

- `pnpm install` (worktree has no node_modules on first run).
- `pnpm type-check` — clean.
- `pnpm test:run` — every test passes. Report the total test count;
  do not predict it.
- `pnpm baseline` — produces docs/baseline-pipeline.md byte-identical
  modulo the `Generated by …` date line (verify with
  `diff -I '^Generated by' docs/baseline-pipeline.md <(git show
  main:docs/baseline-pipeline.md)`). docs/baseline-v3.md MUST remain
  byte-identical to main.
- `pnpm build` — clean (the existing chunk-size advisory on
  dist/assets/index-*.js is unchanged from prior sessions).

Per-consolidation extra checks:
- After U1: `rg "a < b \\? .*\\$\\{a\\}" src/core/` returns exactly one
  hit (in pair-key.ts).
- After U2: `rg "while \\(parent\\[r\\] !== r\\)" src/core/` returns
  exactly one hit (in union-find.ts).
- After U3: `rg "toFixed\\(6\\)" src/core/` returns exactly one hit
  (in intern-vertex.ts).

==============================================================
PR
==============================================================

After all three commits land on the branch and verification passes:

- Push the branch.
- Open PR titled `refactor(core): U1-U3 Pathfinder consolidations (pair-key, union-find, intern-vertex)`.
- PR body uses the project's template; fill the handoff block at the
  bottom (no session log for maintenance work, so the PR body and
  squash-merge commit message together carry the handoff information).
- Wait for CI. Address every CI comment and failure — resolve, or
  reply with a reasoned dismissal.
- Squash-merge to main once CI is green.
- After merge: run `git worktree prune` and delete the worktree.

==============================================================
Out of scope (do NOT do these in this PR)
==============================================================

- U4 (single `runPipeline()` orchestrator). Pathfinder flagged this as
  reversing ADR 0001 and queue-listed it as decision-pending, not
  maintenance. Do not start it.
- Any pipeline behaviour change.
- Any regeneration of docs/baseline-v3.md.
- Any changes to docs/ beyond what an updated test error-message
  assertion forces (U3).

==============================================================
Implementer report — required at PR-open time
==============================================================

In the PR body, briefly note:
- The total test count after all three commits.
- Whether any unit-test error-string assertions were updated in U3,
  and which.
- Any library API or call site discovered to be different from this
  prompt's description — flag for the strategist.
- Anything that surprised you while doing the work.
